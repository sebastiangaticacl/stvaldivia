#!/usr/bin/expect -f

set timeout 30
set host "stvaldivia.cl"
set user "stvaldivia"
set password "Tabxyh-jubvem-5pysxa"

spawn ssh $user@$host
expect {
    "yes/no" { send "yes\r"; exp_continue }
    "password:" { send "$password\r" }
}

expect -re {[\$\#]} 

# Create test file
send "echo 'Hello World Test' > public_html/stvaldivia/test.html\r"
expect -re {[\$\#]}

# Check parent htaccess
send "cat public_html/.htaccess\r"
expect -re {[\$\#]}

# Test static file access
send "curl -I -H 'Host: stvaldivia.cl' http://127.0.0.1/stvaldivia/test.html\r"
expect -re {[\$\#]}

send "exit\r"
expect eof
