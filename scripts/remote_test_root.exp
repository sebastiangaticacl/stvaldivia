#!/usr/bin/expect -f

set timeout 30
set host "stvaldivia.cl"
set user "stvaldivia"
set password "Tabxyh-jubvem-5pysxa"

spawn ssh $user@$host
expect {
    "yes/no" { send "yes\r"; exp_continue }
    "password:" { send "$password\r" }
}

expect -re {[\$\#]} 

# Test if test.html is accessible at root
send "curl -I -H 'Host: stvaldivia.cl' http://127.0.0.1/test.html\r"
expect -re {[\$\#]}

# Also test root path
send "curl -I -H 'Host: stvaldivia.cl' http://127.0.0.1/\r"
expect -re {[\$\#]}

send "exit\r"
expect eof
