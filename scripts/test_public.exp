#!/usr/bin/expect -f

set timeout 30
set host "stvaldivia.cl"
set user "stvaldivia"
set password "Tabxyh-jubvem-5pysxa"

spawn ssh $user@$host
expect {
    "yes/no" { send "yes\r"; exp_continue }
    "password:" { send "$password\r" }
}

expect -re {[\$\#]}

# Go to project dir
send "cd public_html/stvaldivia\r"
expect -re {[\$\#]}

# Create test file in public
send "mkdir -p public\r"
send "echo 'works' > public/test.txt\r"
expect -re {[\$\#]}

# Test access
send "echo '--- TESTING PUBLIC FILE ---'\r"
send "curl -I -H 'Host: stvaldivia.cl' http://127.0.0.1/stvaldivia/test.txt\r"
expect -re {[\$\#]}

send "exit\r"
expect eof
